version: '3.8'

# Simplified docker-compose.yml - remove jmeter service
services:
  influxdb:
    image: influxdb:1.8
    ports:
      - "8087:8086"
    environment:
      - INFLUXDB_DB=jmeter

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3001:3000"